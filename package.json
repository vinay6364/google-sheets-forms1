{
  "name": "form-to-sheets",
  "version": "1.0.0",
  "description": "Form that saves to Google Sheets",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "body-parser": "^1.20.2",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "googleapis": "^120.0.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}